import { ExtensionCount } from "core";
const expectedExtensionsCount: ExtensionCount = {
  editorconfig: 1,
  gitignore: 1,
  json: 5,
  md: 3,
  prettierrc: 1,
  vscodeignore: 1
};

export default expectedExtensionsCount;
